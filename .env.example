# フロントエンド（Next.js）環境変数の例。
# 実際の値は .env.local にコピーして設定する（.env.local は git 管理外）。

# バックエンド API のベース URL。
# 未設定の場合、フロントはモックデータで単体動作する（認証はデモモード）。
# 例: 本番は Docker/クラウド上の API、ローカルは http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
